apiVersion: apps/v1
kind: Deployment

metadata:
  labels:
    type: data-source
    app: coinnews
    kubernetes.app: data-source
  name: coinnews

spec:
  replicas: 1
  selector:
    matchLabels:
      app: coinnews
  template:
    metadata:
      labels:
        app: coinnews
    spec:
      containers:
        - name: coinnews
          image: amirhossein21/coinnews.linux:v0.2
          ports:
            - containerPort: 8000
              name: coinnews
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          volumeMounts:
            - name: config
              mountPath: /src/app/config.yml
              subPath: config.yml
      volumes:
        - name: config
          configMap:
            name: coinnews-configs
